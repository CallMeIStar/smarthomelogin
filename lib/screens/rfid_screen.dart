import 'package:flutter/material.dart';

class RfidScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Rfid')),
      body: Center(child: Text('Rfid')),
    );
  }
}