import 'package:flutter/material.dart';

class VoiceControlScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Voice Control')),
      body: Center(child: Text('Voice Control Page')),
    );
  }
}