import 'package:flutter/material.dart';

class EnergyInfoScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Energy Info')),
      body: Center(child: Text('Energy Info Page')),
    );
  }
}